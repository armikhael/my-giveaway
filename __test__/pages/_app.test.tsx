/* import { render } from "@testing-library/react";
import MyApp from "../../pages/_app";

describe("MyApp", () => {
  test("renders snapshot of app component", () => {
    const wrapper = render(MyApp(<MyApp {...props} />));
    expect(wrapper).toMatchSnapshot();
  });
});
*/
